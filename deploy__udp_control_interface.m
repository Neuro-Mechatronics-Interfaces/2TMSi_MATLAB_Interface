%DEPLOY__UDP_CONTROL_INTERFACE  Deploys GUI for sending UDP messages to SAGA device handler state machine.
fig = init_controller_fig();
waitfor(fig);
disp("[CONTROLLER]::SAGA Device handler state machine UDP interface GUI exited.");
pause(5);
